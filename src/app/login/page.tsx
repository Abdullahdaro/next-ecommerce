const LoginPage = () => {
/*     const { login } = useAuth(); */
    
    return (
        <div>
{/*         <button onClick={login}>Login</button> */}
            <h1>login</h1>
        </div>
    );
    }

export default LoginPage;